<!-- Refer to validation_controller.rb for @result definition -->
<% @result.info_messages.each do |message| %>

  <div class="alert alert-info">
    <%= render partial: 'message', locals: {message: message, validator: @result} %>
  </div>

<% end %>
