<!-- Refer to validation_controller.rb for @result definition -->
<% @result.warnings.each do |warning| %>

  <div class="alert alert-warning">
    <%= render partial: 'message', locals: {message: warning, validator: @result} %>
  </div>

<% end %>
