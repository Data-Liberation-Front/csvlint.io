<!-- Refer to validation_controller.rb for @result definition -->
<% @result.errors.each do |error| %>

  <div class="alert alert-danger">
    <%= render partial: 'message', locals: {message: error, validator: @result} %>
  </div>

<% end %>
