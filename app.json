{
  "name": "csvlint",
  "description": "",
  "scripts": {
    
  },
  "env": {
    "AWS_ACCESS_KEY": {
      "required": true
    },
    "AWS_BUCKET_NAME": {
      "required": true
    },
    "AWS_SECRET_ACCESS_KEY": {
      "required": true
    },
    "CSVLINT_SESSION_SECRET": {
      "generator": "secret"
    },
    "LANG": {
      "required": true
    },
    "RACK_ENV": {
      "required": true
    }
  },
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "free"
    },
    "web": {
      "quantity": 1,
      "size": "free"
    }
  },
  "addons": [
    "mongolab:sandbox",
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/ruby"
    }
  ]
}
